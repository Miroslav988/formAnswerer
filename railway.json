{
  "buildCommand": "cd vue-form-app && npm install && npm run build && cd ../server && npm install",
  "startCommand": "node server/index.js",
  "rootDirectory": ".",
  "env": {
    "PORT": "8080"
  }
}
